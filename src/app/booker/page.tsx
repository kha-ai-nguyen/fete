import BookerDashboard from '@/components/booker/BookerDashboard'

export const metadata = {
  title: 'Booker Dashboard — Fete',
  description: 'Track your venue enquiries, proposals, and shortlist',
}

export default function BookerPage() {
  return <BookerDashboard />
}
